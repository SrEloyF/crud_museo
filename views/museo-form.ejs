<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= museo.id ? 'Editar' : 'Nuevo' %> Museo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container py-5">
    <h1><%= museo.id ? 'Editar' : 'Nuevo' %> Museo</h1>

    <form
      action="/museos<%= museo.id ? '/' + museo.id + '?_method=PUT' : '' %>"
      method="POST"
      class="mt-4"
    >
      <div class="mb-3">
        <label class="form-label">Nombre</label>
        <input
          type="text"
          name="nombre"
          class="form-control"
          value="<%= museo.nombre || '' %>"
          required
        >
      </div>
      <button class="btn btn-success">Guardar</button>
      <a href="/museos" class="btn btn-secondary">Cancelar</a>
    </form>
  </div>
</body>
</html>
